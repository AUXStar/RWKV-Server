```mermaid
graph TD
    Start([启动调度]) --> Init[初始化双工作区<br>mask全True, 创建两个CUDA流]
    Init --> Preload[预装载工作区0<br>update_batch_async mask[0], stream[0]]
    Preload --> Sync0[stream[0]同步完成]
    Sync0 --> Run0[启动工作区0推理<br>task_single_async mask[0], stream[0]]
    Run0 --> Loop{主循环条件<br>存在待处理任务或<br>任一工作区推理未完成?}
    
    Loop -->|是| Check[检查两个工作区推理状态]
    Check --> Query0{stream[0] query?}
    Query0 -->|完成| Collect0[回收工作区0结果<br>collect_finished(0)]
    Query0 -->|未完成| Query1{stream[1] query?}
    Collect0 --> Mark0[标记工作区0空闲]
    Mark0 --> Query1
    
    Query1 -->|完成| Collect1[回收工作区1结果<br>collect_finished(1)]
    Query1 -->|未完成| AnyFree{存在空闲工作区?}
    Collect1 --> Mark1[标记工作区1空闲]
    Mark1 --> AnyFree
    
    AnyFree -->|是| Assign[选择空闲工作区p<br>更新mask[p]中空闲槽位]
    Assign --> Load[装载新任务<br>update_batch_async mask[p], stream[p]]
    Load --> SyncP[stream[p]同步完成]
    SyncP --> RunP[启动工作区p推理<br>task_single_async mask[p], stream[p]]
    RunP --> Sleep[短暂休眠<br>避免CPU空转]
    Sleep --> Loop
    
    AnyFree -->|否| Sleep
    
    Loop -->|否| End([调度结束])
```